[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "mypackage"
version = "0.1.0"
authors = [{name = "Simon Boehme et al."}]
license = {file = "LICENSE.txt"}
description = "My first package, yooo"
dependencies = [
  "numpy",
]

